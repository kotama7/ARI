# Runtime requirements

- `ari-core` and `mcp` installed in the Provider environment.
- `ARI_CHECKPOINT_DIR` for run-frozen requirement/Attestation reads.
- No credential scope and no network access are required.

This Provider is not a Harness driver and is never an execution substrate.
