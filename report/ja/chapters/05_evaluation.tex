% translated-from: en/chapters/05_evaluation.tex@408435f6db3270515ca9cc26283d323defb2ba34c89ae8d8e69a7aab7a794752
% glossary:        5bca8d57a70f021e20c0af79aa06cd21013495c1a8246887ae889cd8a056f5fd
% model:           claude-opus-4-7

\section{評価(保留)}\label{chapter:eval}

\textbf{本章は意図的に短い。本スナップショットでは評価を実施し
ていない。}本レポートの旧草稿は具体的な中央値や葉ごとのスコアを
記載していたが、見直したところ、それらの数値は R3a 執筆時に書か
れた説明用例示であり、凍結された ARI チェックポイントからの計測
ではないことが判明した。そのため、それらは削除した。実在する成
果物のみを残す。

\emph{実装済み}のもの:

\begin{itemize}
  \item コスト計上機構:\texttt{CostTracker}
        (\code{cost\_tracker.py:77})が各モデル呼び出しに対して
        ドルコストを付与する。\texttt{init} は 181 行目でトラ
        ッカをチェックポイントディレクトリに繋ぐ;結果として
        \texttt{cost\_usd} フィールドが \texttt{results.json} に
        実行末に書かれる。
  \item 探索ループは停滞検出器
        (\cref{sec:exploration:termination})により終了する;
        $\Tmax$ / $\Cmax$ ではなく停滞が支配する経験的頻度を本
        章で報告する予定 — スナップショットが揃った時点で。
  \item PGF 図の生成スクリプト
        (\code{shared/figures/scripts/})はシードが固定されてい
        る。\code{make figures} は決定的な合成プレースホルダを再
        生成し \code{check\_figures} を通過させるが、それらのプ
        レースホルダは本文での証拠としては \emph{引用していない}。
\end{itemize}

\emph{未実装}のもの:

\begin{itemize}
  \item ARI v0.7.2 の凍結チェックポイントの確定と、
        \texttt{tree.json} / \texttt{results.json} の
        \code{shared/figures/data/} への抽出。現状、この data ディ
        レクトリには \code{*.meta.yaml} のみが置かれ、いずれも
        \code{source.synthetic: true} とマークされている。
  \item 中央値の実行コスト、シード別の探索曲線、実レビューに基
        づくカテゴリ別ルブリック分布の報告。これらは凍結チェッ
        クポイントを上陸させ集約値を再導出する後続 PR で本章に
        現れる予定である。
  \item 実対象論文 (sc24-00052 TS-SpGEMM、 sc24-00019 cuSZ-i) に
        対する \texttt{paper-re} スキルからのフルロールアウト
        PaperBench 風の再現スコア報告。 これらは各々実 LLM
        completer に対する 12 時間 \texttt{BasicAgent} ロール
        アウトを要し、 オペレーショナル v0.7.3 パスに持ち越す。
\end{itemize}

他章からの相互参照(例えば \cref{sec:exploration:termination} 内)
は、経験的証拠が将来配置される場所を指し示すのみであり、現在置
かれている場所を指してはいない。

v0.7.2 は新 \texttt{execution\_profile} 経路に対する 2 件の
小規模な配管チェックを記録している:対照的な 2 篇の SC24 論文の
preprint に対するルーブリック生成が、 マルチランク HPC 論文に
対しては \texttt{kind:"mpi"}(報告済みの完全なスケール包絡を伴う)
を正しく発行し、 単一マシン論文に対してはフィールドを正しく省略
した;ローカル SLURM パーティションに対するマルチフラグ
\texttt{sbatch} ディスパッチは、 ローカル SLURM バージョンが
対応しない 2 フラグを GRES 可用性プローブと \texttt{sbatch --help}
プローブが drop した後、 キューマネージャに受理された。 これらは
配管の運用上の sanity check であって方法論の測定ではなく、 本文
中の証拠としては引用していない。
