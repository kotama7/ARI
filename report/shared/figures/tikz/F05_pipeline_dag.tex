% F05: pipeline DAG — the four-station paper pipeline in the ARI house
% design language (_style.tex): flat pastel phase fills, aribadge step
% numbers (PaperBench grammar), dashed muted artefact edges, and ONE
% accent back-edge (refine), the figure's single saturated element.
% style.tikzstyles is loaded by shared/preamble.tex / standalone wrapper.
% _style.tex resolves from the build cwd: language builds compile in
% en|ja|zh (../shared/...), the standalone renderer in the repo report
% root (shared/...); same-directory compilation is the last fallback.
\InputIfFileExists{../shared/figures/tikz/_style.tex}{}{%
  \InputIfFileExists{shared/figures/tikz/_style.tex}{}{%
    \input{_style.tex}}}
\begin{tikzpicture}[aricanvas]

  % ---- stage row: four stations, fill encodes the owning phase --------
  \node[aribox idea]                          (idea)   {\figlabel{F5.idea}};
  \node[aribox exp,   right=0.7cm of idea]    (expl)   {\figlabel{F5.exploration}};
  \node[aribox paper, right=of expl]          (paper)  {\figlabel{F5.paper}};
  \node[aribox gate,  right=0.7cm of paper]   (rev)    {\figlabel{F5.review}};

  % ---- macro-flow step badges (instead of edge labels) ----------------
  \node[aribadge] at (idea.north west)  {1};
  \node[aribadge] at (expl.north west)  {2};
  \node[aribadge] at (paper.north west) {3};
  \node[aribadge] at (rev.north west)   {4};

  % ---- band headings: search vs write & review ------------------------
  \node[ariphase, anchor=south west] (hsearch)
        at ([yshift=8mm]idea.north west) {\figlabel{F5.band.search}};
  \pic at ([xshift=-2.5mm]hsearch.west) {ariglyph bulb};
  \node[ariphase, anchor=south west] (hwrite)
        at ([yshift=8mm]paper.north west) {\figlabel{F5.band.write}};
  \pic at ([xshift=-2.5mm]hwrite.west) {ariglyph doc};

  % ---- persisted artefacts, one per producing station -----------------
  \node[aribox, below=0.9cm of expl]  (tree)   {\figlabel{F5.tree_json}};
  \node[aribox, below=0.9cm of paper] (draft)  {\figlabel{F5.draft}};
  \node[aribox, below=0.9cm of rev]   (rubric) {\figlabel{F5.rubric}};

  % ---- primary flow ----------------------------------------------------
  \draw[ariarrow] (idea)  -- (expl);
  \draw[ariarrow] (expl)  -- (paper);
  \draw[ariarrow] (paper) -- (rev);

  % ---- station -> persisted artefact (state) ---------------------------
  \draw[ariarrow muted] (expl)  -- (tree);
  \draw[ariarrow muted] (paper) -- (draft);
  \draw[ariarrow muted] (rev)   -- (rubric);

  % ---- refine back-edge: the one accent element ------------------------
  \draw[ariarrow accent] (rev.north) to[bend right=30]
        node[arinote, pos=0.5] {\figlabel{F5.refine}} (paper.north);

\end{tikzpicture}
