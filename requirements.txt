# ============================================================================
# ARI — Runtime dependencies

# Install: pip install -r requirements.txt
#      or: uv pip install -r requirements.txt
# ============================================================================

# --- Core framework deps (also declared in ari-core/pyproject.toml) ---------
litellm>=1.83.7
pydantic>=2.13.4
typer>=0.26.8
rich>=13.0
pyyaml>=6.0
paramiko>=5.0.0

# --- MCP (skill communication) ---------------------------------------------
mcp>=1.28.1
fastmcp

# --- Memory backend (ari-skill-memory default is Letta) --------------------
letta-client>=0.1

# --- Visualization server ---------------------------------------------------
websockets>=12.0

# --- PDF handling -----------------------------------------------------------
pymupdf>=1.24
pdfminer.six

# --- Visualization / plotting ----------------------------------------------
networkx>=3.0
seaborn>=0.12
matplotlib>=3.7

# --- Skill-specific ---------------------------------------------------------
duckduckgo-search>=6.0
httpx>=0.25
beautifulsoup4>=4.12
arxiv>=2.0
nest-asyncio

# --- v0.7.0: ari-registry server (optional; only needed if you run
#     `ari registry serve`). The publish backend uses urllib so these are
#     not required for `ari ear publish` / `ari clone`.
fastapi>=0.139.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9
