# ari-core/config/profiles

Environment profile overlays (YAML) that adjust defaults per deployment target.

## Contents

- `README.md` — this file.
- `cloud.yaml` — cloud profile overlay.
- `hpc.yaml` — HPC profile overlay.
- `laptop.yaml` — laptop profile overlay.
