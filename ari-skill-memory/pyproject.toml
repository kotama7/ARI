[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "ari-skill-memory"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcp[cli]", "nest-asyncio"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "strict"
