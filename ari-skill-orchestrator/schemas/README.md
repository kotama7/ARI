# Orchestrator contract schemas

These JSON Schemas are deterministic projections of the strict Pydantic models in
`src/ari_skill_orchestrator/contracts.py`.

Regenerate or verify them with:

```bash
python ari-skill-orchestrator/scripts/sync_contracts.py --write
python ari-skill-orchestrator/scripts/sync_contracts.py
```
