# Transform prompts

`science_interpretation.md` is the static, reviewable prompt for the optional
model annotation of canonical node reports. Runtime evidence replaces only the
two `__ARI_*__` sentinels and is bounded before dispatch. Model output remains
outside claim-eligible raw and derived scientific data.
