# Plot prompts

`figure_planner.md` is the bounded, versioned instruction used only to choose
admitted metric IDs and fixed chart modes. It cannot emit numeric values, code,
SVG, paths, captions, or units; those remain deterministic renderer inputs.
