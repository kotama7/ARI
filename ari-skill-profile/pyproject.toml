[project]
name = "ari-skill-profile"
version = "0.1.0"
description = "Read-only ARI hardware-counter profiling Provider"
requires-python = ">=3.13"
dependencies = ["mcp>=1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
